# Trident Frontend — Environment Variables
# 复制为 frontend/.env.local 并修改

# 本地开发：指向本机 FastAPI 后端
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# 生产部署（Nginx 反向代理）：设为空字符串，前端走相对路径
# 所有 /api/* 请求由 Nginx 转发到 FastAPI:8000
# NEXT_PUBLIC_API_URL=
